const items = ["A", "B", "C"];

// For of (Best for values)
for (let item of items) { console.log(item); }

// For in (Gives index)
for (let index in items) { console.log(index); }

// Destructuring
const [first, second, ...rest] = items; 
// first = "A", second = "B", rest = ["C"]