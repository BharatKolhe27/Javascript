let city = "Indore";
console.log(city[0]); // "I"

console.log(city.length);      // 6
console.log(city.toUpperCase()); // "INDORE"
console.log(city.trim());        // IT removes extra spaces
console.log(city.toLowerCase()); 